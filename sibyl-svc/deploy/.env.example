# Copy to deploy/.env ON THE VM and fill in. Never commit the real file.

# Must be byte-identical to SIBYL_SVC_TOKEN in the Vercel project settings.
# Generate: python3 -c "import secrets;print(secrets.token_urlsafe(24))"
STASH_SVC_TOKEN=

# Sibyl account credentials (free tier).
SIBYL_ACCOUNT_ID=
SIBYL_SESSION_TOKEN=
SIBYL_TIER=free
